name: CI

on:
    push:
        branches:
            - main
    pull_request:

jobs:
    sanity:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: Placeholder CI
              run: |
                  echo "CI placeholder – backend/frontend checks will be added once scaffolded."
